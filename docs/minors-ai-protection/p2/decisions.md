# P2 决议记录

## 2026-08-08 — P2 开工

- 触发：P1 冻结为 `p1-v1.0`（数据锚定 v0.4）+ 用户指令继续下一步  
- Owner：Agent-001  
- 决议：

1. 训练/评测数据只读使用 `p1/datasets/v0.4`  
2. 首周交付 sklearn TF-IDF 多标签基线（非最终端侧形态）  
3. 端侧最终形态仍规划为小参数 Transformer → ONNX/TFLite（后续迭代）  
4. 高指标需对照「合成模板同质化」风险；adv 集用于回归，不夸大泛化  
5. 成人业务线继续隔离  

## 2026-08-08 — 换小参数 Transformer

- 触发：用户指令「P2换小参数」
- Owner：Agent-001
- 决议：

1. 主线模型定为 `uer/chinese_roberta_L-2_H-128` 多标签分类头（mean-pool + Linear）
2. TF-IDF 基线保留作对照，不再作为端侧候选
3. 导出格式优先 **ONNX**（opset 17）；体积门槛 ≤30MB 已满足（≈12.1MB）
4. 阈值在 dev 上按 macro F1 网格搜索，当前冻结 **0.4**
5. 时延首版报 CPU ORT 代理；真机 P95 仍属开放项

## 开放项

| ID | 事项 | 状态 |
|---|---|---|
| P2-O1 | 换中文小 Transformer + ONNX 导出 | **已完成**（量化可选后续） |
| P2-O2 | 端侧时延实测（真机） | 待设备 |
| P2-O3 | 与 P3 规则引擎字段契约联调 | 待 P3 |
